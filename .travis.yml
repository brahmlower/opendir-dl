notifications:
  email: false

language: python
python:
  - "2.7"

cache: pip

git:
  depth: 1

install:
  - pip install coveralls
  - pip install nose
  - pip install --use-wheel --no-index --trusted-host opendir-dl.com --find-links http://opendir-dl.com/wheels/lxml-3.6.4-cp27-cp27mu-manylinux1_x86_64.whl lxml
  - pip install .
  
script:
  - make test

after_success:
  coveralls
